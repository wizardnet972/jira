{
  "name": "project-ui-card",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/project/ui/card/src",
  "prefix": "project",
  "projectType": "library",
  "tags": ["domain:project", "type:ui"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/project/ui/card"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
