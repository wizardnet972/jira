{
  "name": "project-ui-header",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/project/ui/header/src",
  "prefix": "project",
  "projectType": "library",
  "tags": ["domain:project", "type:ui"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/project/ui/header"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
