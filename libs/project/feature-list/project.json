{
  "name": "project-feature-list",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/project/feature-list/src",
  "prefix": "project",
  "projectType": "library",
  "tags": ["domain:project", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/project/feature-list"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
