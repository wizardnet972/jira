{
  "name": "project-util-key",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/project/util/key/src",
  "prefix": "project",
  "projectType": "library",
  "tags": ["domain:project", "type:util"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/project/util/key"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
