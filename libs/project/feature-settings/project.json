{
  "name": "project-feature-settings",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/project/feature-settings/src",
  "prefix": "project",
  "projectType": "library",
  "tags": ["domain:project", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/project/feature-settings"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
