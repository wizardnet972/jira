{
  "name": "project-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/project/domain/src",
  "prefix": "project-domain",
  "projectType": "library",
  "tags": ["domain:project", "type:domain-logic"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/project/domain"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
