{
  "name": "board-util-wip",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/board/util/wip/src",
  "prefix": "board",
  "projectType": "library",
  "tags": ["domain:board", "type:util"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/board/util/wip"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
