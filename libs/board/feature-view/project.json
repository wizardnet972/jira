{
  "name": "board-feature-view",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/board/feature-view/src",
  "prefix": "board",
  "projectType": "library",
  "tags": ["domain:board", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/board/feature-view"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
