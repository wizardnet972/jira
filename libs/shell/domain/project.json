{
  "name": "shell-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shell/domain/src",
  "prefix": "shell-domain",
  "projectType": "library",
  "tags": ["domain:shell", "type:domain-logic"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/shell/domain"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
