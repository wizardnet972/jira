{
  "name": "issue-util-key",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/issue/util/key/src",
  "prefix": "issue",
  "projectType": "library",
  "tags": ["domain:issue", "type:util"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/issue/util/key"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
